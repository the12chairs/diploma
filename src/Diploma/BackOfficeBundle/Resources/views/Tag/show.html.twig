{% extends 'DiplomaBackOfficeBundle::layout.html.twig' %}

{% block page -%}

    <h1>Тэг</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Тэг</th>
                <td>{{ entity.title }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('tag') }}">
        К списку
    </a>
    <a class="btn btn-primary" href="{{ path('tag_edit', { 'id': entity.id }) }}">
        Редактировать
    </a>


{% endblock %}
