{% extends 'DiplomaBackOfficeBundle::layout.html.twig' %}
{% block page -%}
    <h1>Question creation</h1>
    <form method="POST" action="{{ path('question_create') }}">
        {{ form_widget(form) }}

        <button type="submit" class="btn btn-success">Сохранить</button>
    </form>
{% endblock %}
