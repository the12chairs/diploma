{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Answer</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Answertext</th>
                <td>{{ entity.answerText }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('answer') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
